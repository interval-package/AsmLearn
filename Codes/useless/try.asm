DATA SEGMENT

PRINT DB "THE FIRST TRY", 0AH, 0DH, '$'

DATA ENDS

STACK SEGMENT
	DW 20 DUP(20)
STACK ENDS

EXTRA SEGMENT

EXTRA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA, SS:STACK, ES:EXTRA
START:
	; Init
	MOV AX, DATA
	MOV DS, AX
	MOV AX, EXTRA
	MOV ES, AX
	MOV AX, STACK
	MOV SS, AX
	MOV SP, 20H

    MOV DX, OFFSET  PRINT

	MOV AX, 0900H
	INT 21H

	MOV AX, 4C00H
	INT 21H

CODE ENDS
END START